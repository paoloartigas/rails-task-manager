<%# <div class="container">
 <h1>My tasks</h1>
   <ul>
     <%@tasks.each do |task| %>
       <%# <li> <i class="fa-regular fa-square<%='-check' if task.completed %>
       <%# </i> <%= link_to task.title, task </li> %>
     <%# <% end %>
   <%# </ul> %>
   <%# <%= link_to "Add a new task", new_task_path %>
<%# </div> %>
<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card shadow-sm">
        <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
          <h5 class="mb-0">My Tasks</h5>
          <%= link_to "Add New Task", new_task_path, class: "btn btn-light btn-sm" %>
        </div>

        <ul class="list-group list-group-flush">
          <% @tasks.each do |task| %>
            <li class="list-group-item d-flex align-items-center justify-content-between">
              <div>
                <i class="fa-regular fa-<%= task.completed ? 'square-check' : 'square' %> me-2"></i>
                <%= link_to task.title, task, class: "text-decoration-none text-dark" %>
              </div>
            
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</div>
